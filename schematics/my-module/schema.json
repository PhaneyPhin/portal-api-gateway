{
    "$schema": "http://json-schema.org/schema",
    "$id": "myModule",
    "title": "NestJS Module Generator",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the module (e.g. permissions)."
      }
    },
    "required": ["name"]
  }